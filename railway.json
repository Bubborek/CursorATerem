{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && cd server && node index.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}