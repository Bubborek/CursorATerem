{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo 'Starting deployment...' && npm run build && echo 'Build completed, starting server...' && cd server && node index.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}